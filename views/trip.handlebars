<div class="container">
    <div class="row">
        <h1>Your Getaway Galaxy trip!</h1>
    </div>
    <div class="row">
        {{!-- for each site selected should populate on the trip page --}}

        {{#each destination}}
        {{> trip/trip-block}}
        {{/each}}

    </div>

    <div class="btnhelper">
        <button type="submit" action="/end" class="btn btn-primary" id="endBtn">End this trip</button>
    </div>
</div>

<script src="js/fav.js"></script>