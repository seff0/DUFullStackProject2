<div class="favorites">
    <div class="divider">
        <h1>YOUR FAVORITE SPACE LOCATIONS</h1>
        <div class="container">
            <div class="row">
                {{#each destination}}
                {{> fav/fav-block}}
                {{/each}}
            </div>
        </div>
    </div>
</div>
<div class="btnhelper">
    <a href="/book">
        <button class="btn btn-primary">Start another trip</button>
    </a>
</div>